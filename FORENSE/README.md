# ForenPy

Ferramenta de triagem forense digital.

## Como usar

```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Execute:
```
python -m forenpy.cli examples/sample_images --ela --ocr --out relatorio.md
```
